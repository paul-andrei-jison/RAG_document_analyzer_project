version: 1
applications:
  - frontend:
      phases:
        build:
          commands:
            - echo "Static site — no build needed"
      artifacts:
        baseDirectory: /
        files:
          - "**/*"
      cache:
        paths: []
    appRoot: docs-site
